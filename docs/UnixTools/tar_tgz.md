# tgz 轉成 tar 檔 <br />

**tar zcvf tarname.tar tgzname.tgz**
<button 
  className="button button--primary" 
  onClick={() => {navigator.clipboard.writeText('tar zcvf tarname.tar tgzname.tgz');alert('已複製指令！');}}>
  複製程式碼
</button>
