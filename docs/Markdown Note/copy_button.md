## 在網頁上有複製按鈕的兩個方法

### 1. 程式碼區塊：<br />
大多數現代的 Markdown 編輯器或平台（如 GitHub、Stack Overflow、Notion、Obsidian）都會在程式碼區塊的右上角**自動添加**一個複製按鈕。你只需要使用「後撇號」（backticks）包裹代碼：

```` 
```python title=" 程式碼區塊 "
print("這段文字在很多平台上會自動出現複製按鈕，而且有加程式碼的標題框")
```
````