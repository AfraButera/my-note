---
id: 2026-05-01_L2_迴圈進階
title: L2｜迴圈進階（2026-05-01）
sidebar_position: 2
---

> 學習日期：2026-05-01
> 單元：階段一 L2 — 迴圈進階
> 狀態：⬜ 習題進行中（習題一 ✅）

---

## 學習重點

### `break` — 提前中斷迴圈

```python
for i in range(10):
    if i == 5:
        break
    print(i)
# 印出 0 1 2 3 4，到 5 就停
```

> 比喻：在箱子裡找鑰匙，找到就不用再翻了。

---

### `continue` — 跳過這次，繼續下一圈

```python
for i in range(10):
    if i % 2 == 0:
        continue
    print(i)
# 只印奇數：1 3 5 7 9
```

> 比喻：掃描清單時看到不符條件的直接略過。

---

### `enumerate` — 同時拿到 index 和值

```python
fruits = ['apple', 'banana', 'cherry']
for i, fruit in enumerate(fruits):
    print(f'{i+1}. {fruit}')
# 1. apple
# 2. banana
# 3. cherry
```

不用手動寫 `i = 0` 再 `i += 1`，`enumerate` 自動計數。

---

### `zip` — 同時走兩個 list

```python
names  = ['Ziv', 'Bobo']
scores = [90, 85]
for name, score in zip(names, scores):
    print(f'{name}：{score} 分')
# Ziv：90 分
# Bobo：85 分
```

> 比喻：把兩份名單像拉鍊一樣配對，每次各取一個。

---

### `while True` + `break` — 直到條件成立才結束

```python
while True:
    age = input('請輸入年齡（數字）：')
    if age.isdigit():
        age = int(age)
        break
    print('請輸入有效數字')
```

最常用在「強制使用者輸入正確格式」的場景。

---

## 習題

### 習題一｜猜數字（✅ 通過）

**題目：** 用 `while True + break` 寫猜數字，隨機產生 1～10，猜中印出共猜幾次

**作答：**

```python
import random

answer = random.randint(1, 10)
userinput = int(input('猜數字一個數字，範圍1-10：'))
times = 1

while True:
    if userinput == answer:
        print('恭喜答對了！您一共猜了', times, '次')
        break
    else:
        userinput = int(input('猜錯了，請再試一次：'))
        times += 1
```

:::note 觀念補充｜while True + break 怎麼運作

一般 while 迴圈的條件寫在開頭，例如：

```python
while userinput != answer:
    ...
```

意思是「每次跑迴圈之前先檢查條件，不成立就停」。

`while True` 是把條件寫死成「永遠成立」，迴圈不會自己停，你必須在裡面決定什麼時候 `break`：

```python
while True:       # ← 永遠進來
    if 條件成立:
        break     # ← 你決定在這裡跳出
    # 否則繼續跑
```

**逐步追蹤（假設 answer = 7）：**

```
userinput = 3，times = 1

【第 1 圈】if 3 == 7？No → 再問，userinput = 9，times = 2
【第 2 圈】if 9 == 7？No → 再問，userinput = 7，times = 3
【第 3 圈】if 7 == 7？Yes → print '猜了 3 次' → break，結束
```

**為什麼用 `while True` 而不是 `while userinput != answer`？**

因為猜數字「不知道要重複幾次」，用 `while True + break` 更清楚——停止的邏輯寫在裡面，讀起來一目瞭然。`while condition` 適合「已知條件」的情況，例如跑固定次數。

:::

---

### 習題二｜enumerate 選單（⬜ 進行中）

**題目：** 用 `enumerate` 印出選單，選到「離開」用 `break` 結束

---

### 習題三｜zip 對應（⬜ 進行中）

**題目：** `items = ['咖啡', '蛋糕', '果汁']`、`prices = [60, 80, 50]`，用 `zip` 印出每項名稱與價格，最後印總金額
